export function PrivacyNotice() {
  return (
    <p className="mx-auto max-w-xl text-center text-xs leading-relaxed text-muted">
      Your photo is only used to create this visualisation. It is never stored longer than
      necessary, never shared publicly, and never used to train AI models.
    </p>
  );
}
